spring.application.name=forum-hub

spring.datasource.url=jdbc:mysql://localhost:3306/forumhub
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PSWD}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# depois dos : eh um valor default:
api.security.token.secret=${JWT_SECRET:12345678}

spring.output.ansi.enabled=always

#spring.flyway.repair.enabled=true
#springdoc.api-docs.path=/api-docs
#springdoc.swagger-ui.path=/swagger-ui.html

# talvez seja necessário usar isso se houver algum problema com a conexão do banco de dados:
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#caso quisesse deixar os parametros da query de outra forma:
#spring.data.web.pageable.page-parameter=pagina
#spring.data.web.pageable.size-parameter=tamanho
#spring.data.web.sort.sort-parameter=ordem